[pytest]
testpaths = tests
addopts = -q
markers =
    pipeline(name): mark a test as belonging to a Kedro pipeline.
    model_stage(stage): mark a model test for a specific phase (pre_training, post_training, invariance, directional, unit).